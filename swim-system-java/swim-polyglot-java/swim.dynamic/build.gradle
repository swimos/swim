apply from: '../project.gradle'

description = 'Language binding interfaces'
ext.moduleName = 'swim.dynamic'

dependencies {
  implementation group: 'org.swimos', name: 'swim-collections', version: version
}
