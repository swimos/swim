apply from: '../project.gradle'

description = 'Swim authenticator runtime'
ext.moduleName = 'swim.auth'

dependencies {
  api group: 'ai.swim', name: 'swim-security', version: version
  api group: 'ai.swim', name: 'swim-io-http', version: version
  api project(':swim-kernel')
}
