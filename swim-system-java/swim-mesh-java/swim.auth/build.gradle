apply from: '../project.gradle'

description = 'Swim authenticator runtime'
ext.moduleName = 'swim.auth'

dependencies {
  api group: 'org.swimos', name: 'swim-security', version: version
  api group: 'org.swimos', name: 'swim-io-http', version: version
  api project(':swim-kernel')
}
