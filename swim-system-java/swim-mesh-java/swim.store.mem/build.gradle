apply from: '../project.gradle'

description = 'In-memory storage runtime'
ext.moduleName = 'swim.store.mem'

dependencies {
  api group: 'org.swimos', name: 'swim-collections', version: version
  api group: 'org.swimos', name: 'swim-spatial', version: version
  api project(':swim-store')
  api project(':swim-kernel')
}
